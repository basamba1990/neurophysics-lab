# Fichier: backend/.env.example (Version Corrigée pour Production)
# Ce fichier NE DOIT PAS contenir de clés réelles. Il sert de modèle.
# Les valeurs réelles doivent être stockées dans les secrets de Vercel ou de votre hôte de backend.

# --- Configuration de l'Application ---
# La clé secrète doit être générée aléatoirement (e.g., via 'python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"')
# Elle est utilisée pour la signature des tokens JWT et la sécurité des sessions.
SECRET_KEY="VOTRE_CLE_SECRETE_DE_PRODUCTION_TRES_LONGUE_ET_ALEATOIRE"

# Définir l'environnement pour les logs et le comportement de l'application
# Doit être 'production' pour un déploiement réel
ENVIRONMENT="production"

# --- Configuration de la Base de Données (Supabase) ---
# Clé anonyme (utilisée par le frontend, mais aussi utile pour certaines opérations backend)
SUPABASE_URL="https://votre-instance.supabase.co"
SUPABASE_ANON_KEY="VOTRE_CLE_ANON_SUPABASE"

# Clé de rôle de service (CRITIQUE : accès complet à la DB, doit être UNIQUEMENT côté serveur)
SUPABASE_SERVICE_KEY="VOTRE_CLE_SERVICE_SUPABASE_TRES_SECRETE"

# --- Services Tiers ---
# Clé d'API OpenAI pour le Copilot Scientifique
OPENAI_API_KEY="VOTRE_CLE_OPENAI_API"
OPENAI_ORGANIZATION="VOTRE_ORGANISATION_OPENAI"

# Configuration Redis (Upstash ou autre) pour la mise en cache et les tâches asynchrones
UPSTASH_REDIS_REST_URL="https://votre-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="VOTRE_TOKEN_UPSTASH_SECRETS"

# --- Configuration CORS ---
# Liste des origines autorisées séparées par des virgules (e.g., "https://neurophysics-lab.vercel.app,https://preview.vercel.app")
# Utilisez "*" uniquement pour le développement local.
ALLOWED_ORIGINS="https://neurophysics-lab.vercel.app"
